# Python 3.11 x64。torch/torchaudio 与 transkun 在打包脚本中单独安装：
# - torch 使用官方 CPU wheel 源，避免 CUDA 运行时进入安装包；
# - transkun 使用 --no-deps，排除官方 CLI 的 pydub/ffmpeg 路径。
moduleconf==0.1.4
numpy==2.1.3
scipy==1.15.2
pretty-midi==0.2.11
mir-eval==0.8.2
miniaudio==1.71
pyinstaller==6.21.0
soundfile==0.13.1
