## 变更内容

（本 PR 解决了什么；一个 PR 只对应一个 Issue）

## 设计说明

（关键设计决策与 docs/adr/ 的对应关系）

## 测试结果

- [ ] `cargo fmt --check` / `cargo clippy --workspace --all-targets --all-features` / `cargo test --workspace` 通过
- [ ] `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` 通过
- [ ] CI 全部通过

## 截图或录屏

（UI 变更必须附；无 UI 变更可删除本节）

## 风险

（合规、安全、性能、兼容性影响）

## 回滚方式

（如何回退此变更）

## 检查清单

- [ ] 关联 Issue：Closes #<n>
- [ ] 未包含无关改动
- [ ] 无密钥/本机路径/模型权重入库
- [ ] 无违规代码（注入/反作弊/隐藏自动化等红线）
- [ ] 测试覆盖新增/修改逻辑
